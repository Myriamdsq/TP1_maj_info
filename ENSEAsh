#include <unistd.h>

int main() {
    const char message[] = "Bonjour, monde!\n";
    
    // Utilisation de la fonction système write pour afficher le message
    write(STDOUT_FILENO, message, sizeof(message) - 1);

    return 0;
}
